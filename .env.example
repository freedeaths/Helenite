# Vault 配置示例
# 复制此文件为 .env.local 并根据需要修改

# Vault 在 public 目录下的路径
# 默认值: /vault/Publish
VITE_VAULT_PATH=/vault/Publish

# Vault 根目录（通常是 VITE_VAULT_PATH 的父目录）
# 默认值: /vault
VITE_VAULT_ROOT=/vault

# Cusdis 评论系统配置（可选）
# VITE_CUSDIS_APP_ID=your-cusdis-app-id
# VITE_CUSDIS_HOST=https://cusdis.com

# Docker Compose 配置
# 如果你想在 Docker 中使用不同的路径，可以设置这些变量
# 然后运行: docker compose up -d
# 注意：修改后需要重新构建镜像: docker build -t helenite:latest .
